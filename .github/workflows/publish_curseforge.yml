name: Publish (CurseForge)

on:
  workflow_dispatch:
    inputs:
      module:
        description: 'Module to publish'
        required: true
        default: 'core'
        type: choice
        options:
          - core
          - addon-turtles
          - addon-sable
          - addon-create

jobs:
  curseforge:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-encryption-key: ${{ secrets.GRADLE_ENCRYPTION_KEY }}

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'

      - name: Build ${{ inputs.module }}
        run: ./gradlew -p ${{ inputs.module }} build

      - name: Publish ${{ inputs.module }} to CurseForge
        run: ./gradlew :${{ inputs.module }}:publishCurseforge
        env:
          CURSEFORGE_TOKEN: ${{ secrets.CURSEFORGE_TOKEN }}
          CURSEFORGE_PROJECT_BLUEMAP3D: ${{ secrets.CURSEFORGE_PROJECT_BLUEMAP3D }}
          CURSEFORGE_PROJECT_TURTLES: ${{ secrets.CURSEFORGE_PROJECT_TURTLES }}
          CURSEFORGE_PROJECT_SABLE: ${{ secrets.CURSEFORGE_PROJECT_SABLE }}
          CURSEFORGE_PROJECT_CREATE: ${{ secrets.CURSEFORGE_PROJECT_CREATE }}
