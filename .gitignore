# eclipse
bin
*.launch
.settings
.metadata
.classpath
.project

# idea
out
*.ipr
*.iws
*.iml
.idea/*
!.idea/scopes

# gradle
build
.gradle

# other
eclipse
run
logs

# Steam 'n' Rails publishes no maven, so its jar is dropped in by hand. The other two are
# a scratch drop point the build no longer reads - prepareDevServer looks in libs/ for a
# BlueMap jar, which is not on maven either.
addon-sable/libs
addon-create/libs
dev-harness/libs

# docs is scratch space - images, notes, anything not part of the repo
docs

# Scratch: SDD workspace, and stray run logs
.superpowers
*.log
